extends layout

block content
  form(method="post", enctype="multipart/form-data")
    input(type="file", name="image")
    input(type="submit")
  h3= message
  each href in hrefs ? hrefs : []
    img(src=href)